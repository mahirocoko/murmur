---
import { cn } from '../../lib/utils';

type Props = {
  class?: string;
};

const { class: className } = Astro.props;
---
<span class={cn('inline-flex items-center rounded-sm border border-border bg-secondary px-2 py-1 text-xs font-medium text-muted', className)}>
  <slot />
</span>
